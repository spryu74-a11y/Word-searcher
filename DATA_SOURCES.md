# Data Sources

The app loads `data/default-dictionary.js`, generated by `tools/build_dictionary_pack.py`.

Included sources:

- Korean: `spellcheck-ko/hunspell-dict-ko` release `0.7.94`, `ko.dic`.
- English: `dwyl/english-words`, `words_alpha.txt`.
- Local overrides: `data/extra_terms.txt`.
- User word pack: `data/korean_words_natural_common_2023.txt`.
- Official user word pack: `data/official_words_big_sorted_cleaner.txt`.
- Dictionary user word pack: `data/korean_dictionary_words_cleaned.txt`.
- Archaic-inclusive dictionary user word pack: `data/korean_dictionary_words_with_archaic.txt`.
- Dialect and regional Korean word pack: `data/dialect_terms.txt`. This pack also keeps user-reported missing dialect or rare-start words such as `윰차`, `낏내`, and `자갈왓` out of the weak-source filter.
- Open dictionary extra word pack: `data/korean_words_opendict_extra.txt`.
- Naver dictionary word pack: `data/korean_words_with_naver_dict.txt`.
- Woori Mal Sam full JSON word pack: `data/woorimalsam_words.txt`, generated from the local full JSON download with `tools/extract_woorimalsam_words.py`. The extractor keeps `word_unit = 어휘`, removes phrase entries, suffix/prefix particles, endings, and non-Hangul headwords, and keeps dialect, archaic, and North Korean senses.
- Standard Korean Dictionary full JSON word pack: `data/pyojun_words.txt`, generated from `Normalstudent1/pyojunDict` or another local Standard Korean Dictionary full JSON download with `tools/extract_pyojun_words.py`. The extractor keeps `word_unit = 단어`, removes phrases, endings, particles, affixes, one-syllable headwords, homograph numbers, hyphen/caret separators, and non-Hangul headwords.
- Optional Standard Korean Dictionary Open API word pack: `data/stdict_api_words.txt`, generated with `tools/fetch_stdict_api_words.py`. The fetcher uses `https://stdict.korean.go.kr/api/search.do`, `req_type=json`, `advanced=y`, `target=1`, `method=start`, and `type1=word`, then recursively splits prefixes when API pagination limits would otherwise hide results. It stores progress in `data/stdict_api_fetch_state.json`.
- Cleaned dictionary word pack: `data/korean_words_dictionary_cleaned.txt`.
- Wordrow one-shot word pack extracted from `data/wordrow_all_starts_ends.md`: `data/wordrow_oneshot_terms.txt`.
- Wordrow merged dictionary word pack: `data/korean_words_dictionary_wordrow_merged.txt`, included only for entries already present in trusted local sources, the Woori Mal Sam pack, or the extracted Wordrow one-shot pack.
- Competitive exclusions: `data/excluded_terms.txt`.
- Default-pack filter: weak bulk packs are included only for entries already present in the trusted local dictionary sources.
- Surface-form filter: Hunspell surface-form flags are skipped, and entries that resolve to an existing base form are removed so forms like `주름져`, `자빠져`, `자유롭`, `내디뎌`, `맞춰`, `골라`, `몽띠쥬`, `이쁘쥬`, and `공부해요` are not imported as standalone words. The same filter is applied when custom, file, or online words are merged into an existing dictionary.

Archaic, North Korean, and dialect/regional words from the trusted local dictionary sources are kept in the default build.

Attribution:

- Source: Woori Mal Sam (`출처:우리말샘`).
- Woori Mal Sam copyright policy: `https://opendict.korean.go.kr/service/copyrightPolicy`.
- Unless otherwise noted, Woori Mal Sam text materials are distributed under CC BY-SA 2.0 Korea: `https://creativecommons.org/licenses/by-sa/2.0/kr/deed.ko`.

Runtime dictionary:

- The browser app is fully offline. It loads only `data/default-dictionary.js` plus optional user-added text/file entries.
- Woori Mal Sam is the primary source for the bundled offline word pack. Its Open API and full-download JSON are build-time inputs only; the browser app does not call Woori Mal Sam, Wordrow, Wiktionary, or any other network source while searching.
- Any missed word should be added through `data/extra_terms.txt` or a custom word file, then `tools/build_dictionary_pack.py` should be run to regenerate the bundled dictionary.

Rebuild:

```powershell
python .\tools\extract_woorimalsam_words.py --source-dir "C:\Users\spryu\Downloads\전체 내려받기_우리말샘_json_20260603"
python .\tools\extract_pyojun_words.py --source-dir "C:\tmp\pyojunDict\전체 내려받기_표준국어대사전_JSON_20230906"
python .\tools\build_dictionary_pack.py
```

Fetch Standard Korean Dictionary words with an API key:

```powershell
$env:STDICT_API_KEY = "your-32-hex-key"
python .\tools\fetch_stdict_api_words.py
python .\tools\build_dictionary_pack.py
```

Extra files can be merged:

```powershell
python .\tools\build_dictionary_pack.py --extra .\data\my_terms.txt
```

Extra line formats:

```text
단어
english
Secret=시크릿
benzene=벤젠
```

PubChem-scale chemical synonym dumps are intentionally not bundled. They are too large for a browser-first static app, but filtered alphabetic synonym files can be merged with `--extra`.
